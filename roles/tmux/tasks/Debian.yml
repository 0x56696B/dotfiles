---
- name: "Tmux | {{ ansible_distribution }} | Dependencies"
  ansible.builtin.apt:
    name:
      - tmux
      - fzf
      - wl-clipboard
      - rust-fd-find
    state: present
  become: true
  become_user: root
